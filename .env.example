# Copy values into ~/.claude/.env (preferred) or a skill-local .env. Never commit real keys.
SERPER_API_KEY=        # https://serper.dev  - Google SERP (search | news | scholar)
BRAVE_API_KEY=         # https://api.search.brave.com - independent web index (web | news)
JINA_API_KEY=          # https://jina.ai - s.jina.ai search + r.jina.ai reader
